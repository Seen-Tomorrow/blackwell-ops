{
  "identifier": "default",
  "description": "Default capability for the Tauri app",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-set-theme",
    "shell:default",
    "mcp-bridge:default"
  ]
}
