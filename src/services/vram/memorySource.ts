/**
 * Compat re-export — measured-only SOURCE in ./forecast/memorySource.
 * Formula path archived under tmp/archive_vram_formula/.
 */
export {
  attachMemorySource,
  resolveMemorySource,
  MEMORY_SOURCE_LABELS,
  MEMORY_SOURCE_ACCENT,
} from "./forecast/memorySource";
